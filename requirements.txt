# ============================================================
#  Génération LOCALE d'images (generate_lora.py) — voie principale
# ============================================================
# torch/torchvision en build CUDA pour la RTX 4060 (driver CUDA 13.x -> cu130).
# IMPORTANT : installer avec l'index ci-dessous, sinon pip prend la version CPU.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.12.1+cu130
torchvision==0.27.1+cu130

diffusers==0.38.0
transformers==4.57.6        # garder < 5 : transformers 5.x casse from_single_file
huggingface_hub==0.36.2
accelerate==1.14.0
peft==0.19.1                # requis pour load_lora_weights / set_adapters
safetensors==0.8.0
pillow==12.2.0
ultralytics==8.3.0          # détecteurs YOLO (mains/visage) pour retouch_solle.py

# ============================================================
#  API Gemini / nano-banana (api.py, main.py, src/) — optionnel, PAYANT
# ============================================================
google-genai==2.10.0
python-dotenv==1.2.2
fastapi
uvicorn[standard]
